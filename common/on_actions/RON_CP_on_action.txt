on_actions = {
	on_weekly = {
		effect = {
			if = {
				limit = {
					date < 1920.1.1
				}
				every_country = {
					limit = {
						has_country_flag = ww1_famine_risk
						#has_global_flag = ENG_blockade
					}
					#calculate the new consumption
				
					set_temp_variable = { num_manp = max_manpower_k }
					multiply_temp_variable = { num_manp = 0.07} 
					set_variable = { weekly_grain_consumption = num_manp}
			
					#calculate the new stockpile
					set_temp_variable = { temp1 = weekly_grain_consumption }
					substract_from_variable = { grain_stockpile = temp1 }
				}
			}
		}
	}

}