
RON_Wars_Early  = { 
engvictoryinpanamacanal = {

		icon = generic_industry
		
		allowed = { tag = ENG }   
		available = {
			tag = ENG
			USA = { surrender_progress > 0.8 }
			has_global_flag = DNCwarpana
		}

		visible = {
			date < 1920.1.1
			has_war_with = USA
			tag = ENG
						has_global_flag = DNCwarpana

		}

		fire_only_once = yes

		cost = 15

		complete_effect = {
			country_event= {
days = 1 
id = eoanbm.38
}
hidden_effect = {
			every_country = {
             if = {
                 limit = { is_in_faction_with = ENG }
                 white_peace = USA
             }
        }

			}
		}
		ai_will_do = {
			factor = 100
		}
	}
	
	
	
	
	
	usavictoryinpanamacanal = {

		icon = generic_industry
		
		allowed = { tag = ENG }   
		available = {
			tag = ENG
			USA = { surrender_progress > 0.8 }
			has_global_flag = DNCwarpana
		}

		visible = {
			date < 1920.1.1
			has_war_with = USA
			tag = ENG
						has_global_flag = DNCwarpana

		}

		fire_only_once = yes

		cost = 15

		complete_effect = {
			country_event= {
days = 1 
id = eoanbm.38
}
hidden_effect = {
			every_country = {
             if = {
                 limit = { is_in_faction_with = ENG }
                 white_peace = USA
             }
        }

			}
		}
		ai_will_do = {
			factor = 100
		}
	}