########################################################################################
# AUSTRIA-HUNGARY - THE WAY OUT OF A NATIONALITY CRISIS
########################################################################################
# each settlement converts the claim
# into another legal title, and bills it
# to a nationality named in the tooltip

AUH_settlement_prevention = {

	# Maehrischer Ausgleich, 1905
	AUH_settlement_moravian_cadastre = {

		icon = GFX_decision_generic_form_nation
		priority = 90
		cost = 90
		days_remove = 0
		fire_only_once = yes

		allowed = {
			original_tag = AUH
		}

		visible = {
			tag = AUH
			NOT = { has_idea = AUH_settlement_the_moravian_cadastre }
		}

		available = {
			custom_trigger_tooltip = {
				check_variable = { CZE_support > 44 }
				tooltip = AUH_settlement_czech_still_talking_tt
			}
			check_variable = { AUS_support > 39 }
			has_stability > 0.40
		}

		complete_effect = {
			add_ideas = AUH_settlement_the_moravian_cadastre
			add_stability = 0.05
			add_to_variable = { CZE_support = 10 }
			subtract_from_variable = { AUS_support = 6 }
			add_to_variable = { AUH_gemeinsamkeit = 3 }
			every_owned_state = {
				limit = {
					is_core_of = CZE
					is_controlled_by = AUH
				}
				add_compliance = 5
			}
			custom_effect_tooltip = AUH_settlement_moravian_cadastre_tt
			hidden_effect = {
				set_variable = { AUH_news_id = 600 }
				set_variable = { AUH_news_scope = 2 }
				AUH_headline = yes
			}
			AUH_settlement_bound_the_registers = yes
		}

		ai_will_do = {
			factor = 60
		}
	}

	# Czernowitz votes it, 16 October 1909
	AUH_settlement_bukovina_seats = {

		icon = GFX_decision_generic_election
		priority = 89
		cost = 80
		days_remove = 0
		fire_only_once = yes

		allowed = {
			original_tag = AUH
		}

		visible = {
			tag = AUH
			NOT = { has_idea = AUH_settlement_the_bucovina_seats }
		}

		available = {
			date > 1906.1.1
			custom_trigger_tooltip = {
				check_variable = { UKR_support > 44 }
				tooltip = AUH_settlement_ruthene_still_talking_tt
			}
			check_variable = { ROM_support > 39 }
		}

		complete_effect = {
			add_ideas = AUH_settlement_the_bucovina_seats
			add_stability = 0.04
			add_to_variable = { UKR_support = 9 }
			add_to_variable = { ROM_support = 7 }
			subtract_from_variable = { POL_support = 5 }
			subtract_from_variable = { AUS_support = 4 }
			add_to_variable = { AUH_gemeinsamkeit = 2 }
			add_popularity = {
				ideology = democratic
				popularity = 0.03
			}
			custom_effect_tooltip = AUH_settlement_bukovina_seats_tt
			hidden_effect = {
				set_variable = { AUH_news_id = 601 }
				set_variable = { AUH_news_scope = 2 }
				AUH_headline = yes
			}
			AUH_settlement_bound_the_registers = yes
		}

		ai_will_do = {
			factor = 50
		}
	}

	# 63 seats of 425, and no majority without
	AUH_settlement_polish_club_in_office = {

		icon = GFX_decision_generic_political_discourse
		priority = 88
		cost = 70
		days_remove = 0
		fire_only_once = yes

		allowed = {
			original_tag = AUH
		}

		visible = {
			tag = AUH
			NOT = { has_idea = AUH_settlement_the_club_in_office }
		}

		available = {
			custom_trigger_tooltip = {
				check_variable = { POL_support > 44 }
				tooltip = AUH_settlement_pole_still_talking_tt
			}
		}

		complete_effect = {
			add_ideas = AUH_settlement_the_club_in_office
			add_stability = 0.06
			add_to_variable = { POL_support = 12 }
			subtract_from_variable = { UKR_support = 8 }
			add_to_variable = { AUH_gemeinsamkeit = 2 }
			custom_effect_tooltip = AUH_settlement_polish_club_tt
			AUH_settlement_bound_the_registers = yes
		}

		ai_will_do = {
			factor = 70
		}
	}

	# sanctioned 26 January 1907
	AUH_settlement_cisleithanian_franchise = {

		icon = GFX_decision_generic_election
		priority = 87
		cost = 130
		days_remove = 0
		fire_only_once = yes

		allowed = {
			original_tag = AUH
		}

		visible = {
			tag = AUH
			NOT = { has_idea = AUH_settlement_the_franchise_of_1907 }
		}

		available = {
			custom_trigger_tooltip = {
				check_variable = { AUS_support > 44 }
				tooltip = AUH_settlement_german_still_talking_tt
			}
			has_political_power > 129
		}

		complete_effect = {
			add_ideas = AUH_settlement_the_franchise_of_1907
			add_stability = 0.05
			add_war_support = 0.04
			add_to_variable = { CZE_support = 8 }
			add_to_variable = { POL_support = 6 }
			add_to_variable = { UKR_support = 8 }
			add_to_variable = { SLV_support = 8 }
			add_to_variable = { ITA_support = 6 }
			subtract_from_variable = { AUS_support = 12 }
			subtract_from_variable = { AUH_gemeinsamkeit = 4 }
			add_popularity = {
				ideology = democratic
				popularity = 0.06
			}
			# 63 of 425 becomes 63 of 516 overnight
			if = {
				limit = { has_idea = AUH_settlement_the_club_in_office }
				subtract_from_variable = { POL_support = 5 }
				add_political_power = -40
			}
			custom_effect_tooltip = AUH_settlement_cisleithanian_franchise_tt
			hidden_effect = {
				set_variable = { AUH_news_id = 603 }
				set_variable = { AUH_news_scope = 3 }
				AUH_headline = yes
			}
			AUH_settlement_bound_the_registers = yes
		}

		ai_will_do = {
			factor = 40
		}
	}

	# the school clauses of 1907 lapse
	AUH_settlement_school_laws_suspended = {

		icon = GFX_decision_generic_book
		priority = 86
		cost = 100
		days_remove = 0
		fire_only_once = yes

		allowed = {
			original_tag = AUH
		}

		visible = {
			tag = AUH
			NOT = { has_idea = AUH_settlement_the_school_laws_suspended }
		}

		available = {
			custom_trigger_tooltip = {
				check_variable = { SLO_support > 34 }
				tooltip = AUH_settlement_slovak_reachable_tt
			}
			check_variable = { HUN_support > 39 }
		}

		complete_effect = {
			add_ideas = AUH_settlement_the_school_laws_suspended
			add_stability = 0.04
			add_to_variable = { SLO_support = 14 }
			add_to_variable = { ROM_support = 6 }
			add_to_variable = { SER_support = 4 }
			subtract_from_variable = { HUN_support = 10 }
			# Budapest reads it as a broken Compromise
			if = {
				limit = { check_variable = { HUN_support < 30 } }
				subtract_from_variable = { AUH_gemeinsamkeit = 4 }
			}
			custom_effect_tooltip = AUH_settlement_school_laws_tt
			AUH_settlement_bound_the_registers = yes
		}

		ai_will_do = {
			factor = 40
		}
	}

	# Innsbruck kept its faculty one day, 1904
	AUH_settlement_italian_faculty = {

		icon = GFX_decision_generic_book
		priority = 85
		cost = 80
		days_remove = 0
		fire_only_once = yes

		allowed = {
			original_tag = AUH
		}

		visible = {
			tag = AUH
			NOT = { has_idea = AUH_settlement_the_italian_faculty }
		}

		available = {
			custom_trigger_tooltip = {
				check_variable = { ITA_support > 29 }
				tooltip = AUH_settlement_italian_reachable_tt
			}
		}

		complete_effect = {
			add_ideas = AUH_settlement_the_italian_faculty
			add_to_variable = { ITA_support = 14 }
			subtract_from_variable = { AUS_support = 8 }
			subtract_from_variable = { SLV_support = 5 }
			add_tech_bonus = {
				name = AUH_settlement_faculty_bonus
				bonus = 1.0
				uses = 1
				category = electronics
			}
			random_owned_controlled_state = {
				limit = { is_core_of = ITA }
				add_extra_state_shared_building_slots = 1
			}
			custom_effect_tooltip = AUH_settlement_italian_faculty_tt
			AUH_settlement_bound_the_registers = yes
		}

		ai_will_do = {
			factor = 35
		}
	}

	# Vienna pays for what Budapest forbids
	AUH_settlement_slovak_school_fund = {

		icon = GFX_decision_generic_civil_support
		priority = 84
		cost = 50
		days_remove = 30
		fire_only_once = no

		allowed = {
			original_tag = AUH
		}

		visible = {
			tag = AUH
		}

		available = {
			custom_trigger_tooltip = {
				check_variable = { SLO_support < 100 }
				tooltip = AUH_settlement_slovak_register_open_tt
			}
		}

		complete_effect = {
			add_to_variable = { SLO_support = 15 }
			subtract_from_variable = { HUN_support = 4 }
			custom_effect_tooltip = AUH_settlement_slovak_school_fund_tt
			AUH_settlement_bound_the_registers = yes
		}

		ai_will_do = {
			factor = 120
		}
	}

	# conceded town by town in the Littoral
	AUH_settlement_littoral_language_decree = {

		icon = GFX_decision_generic_civil_support
		priority = 83
		cost = 50
		days_remove = 30
		fire_only_once = no

		allowed = {
			original_tag = AUH
		}

		visible = {
			tag = AUH
		}

		available = {
			custom_trigger_tooltip = {
				check_variable = { ITA_support < 100 }
				tooltip = AUH_settlement_italian_register_open_tt
			}
		}

		complete_effect = {
			add_to_variable = { ITA_support = 15 }
			subtract_from_variable = { AUS_support = 4 }
			custom_effect_tooltip = AUH_settlement_littoral_decree_tt
			AUH_settlement_bound_the_registers = yes
		}

		ai_will_do = {
			factor = 120
		}
	}
}

AUH_settlement_compromise = {

	# Lemberg, 14 February 1914
	AUH_settlement_galician_compromise = {

		icon = GFX_decision_generic_scales
		priority = 80
		cost = 120
		days_remove = 0
		fire_only_once = yes

		allowed = {
			original_tag = AUH
		}

		visible = {
			tag = AUH
			NOT = { has_idea = AUH_settlement_the_galician_seats }
		}

		available = {
			custom_trigger_tooltip = {
				check_variable = { UKR_support < 46 }
				tooltip = AUH_settlement_ruthene_grievance_tt
			}
			check_variable = { UKR_support > 14 }
			check_variable = { POL_support > 29 }
		}

		complete_effect = {
			add_ideas = AUH_settlement_the_galician_seats
			add_stability = 0.04
			add_to_variable = { UKR_support = 18 }
			subtract_from_variable = { POL_support = 12 }
			every_owned_state = {
				limit = {
					is_core_of = UKR
					is_controlled_by = AUH
				}
				add_compliance = 8
			}
			# the Club invoices this one
			if = {
				limit = { has_idea = AUH_settlement_the_club_in_office }
				subtract_from_variable = { POL_support = 6 }
				add_political_power = -60
			}
			custom_effect_tooltip = AUH_settlement_galician_compromise_tt
			hidden_effect = {
				set_variable = { AUH_news_id = 602 }
				set_variable = { AUH_news_scope = 2 }
				AUH_headline = yes
			}
			AUH_settlement_bound_the_registers = yes
		}

		ai_will_do = {
			factor = 50
		}
	}

	# Bohemia refused the model in 1913
	AUH_settlement_bohemian_cadastre = {

		icon = GFX_decision_generic_scales
		priority = 79
		cost = 140
		days_remove = 0
		fire_only_once = yes

		allowed = {
			original_tag = AUH
		}

		visible = {
			tag = AUH
			# it must have worked in one province
			OR = {
				has_idea = AUH_settlement_the_moravian_cadastre
				has_idea = AUH_settlement_the_bucovina_seats
				has_idea = AUH_settlement_the_galician_seats
			}
			NOT = { has_idea = AUH_settlement_the_bohemian_cadastre_signed }
		}

		available = {
			custom_trigger_tooltip = {
				check_variable = { CZE_support < 46 }
				tooltip = AUH_settlement_czech_grievance_tt
			}
			check_variable = { CZE_support > 14 }
		}

		complete_effect = {
			custom_effect_tooltip = AUH_settlement_bohemian_talks_tt
			country_event = { id = auh_settlement.1 hours = 6 }
		}

		ai_will_do = {
			factor = 45
		}
	}

	# thirty seats, the courts, one gymnasium
	AUH_settlement_transylvanian_talks = {

		icon = GFX_decision_generic_political_discourse
		priority = 78
		cost = 110
		days_remove = 0
		fire_only_once = yes

		allowed = {
			original_tag = AUH
		}

		visible = {
			tag = AUH
			NOT = { has_idea = AUH_settlement_the_transylvanian_terms }
		}

		available = {
			custom_trigger_tooltip = {
				check_variable = { ROM_support < 46 }
				tooltip = AUH_settlement_romanian_grievance_tt
			}
			check_variable = { ROM_support > 14 }
			check_variable = { HUN_support > 24 }
		}

		complete_effect = {
			custom_effect_tooltip = AUH_settlement_transylvanian_talks_tt
			country_event = { id = auh_settlement.2 hours = 6 }
		}

		ai_will_do = {
			factor = 55
		}
	}

	# a ban, a quashed verdict, a pardon
	AUH_settlement_zagreb_reversed = {

		icon = GFX_decision_generic_scales
		priority = 77
		cost = 100
		days_remove = 0
		fire_only_once = yes

		allowed = {
			original_tag = AUH
		}

		visible = {
			tag = AUH
			NOT = { has_idea = AUH_settlement_the_ban_restored }
		}

		available = {
			custom_trigger_tooltip = {
				check_variable = { CRO_support < 46 }
				tooltip = AUH_settlement_croat_grievance_tt
			}
			check_variable = { CRO_support > 14 }
		}

		complete_effect = {
			add_ideas = AUH_settlement_the_ban_restored
			add_stability = 0.05
			add_to_variable = { CRO_support = 14 }
			add_to_variable = { SER_support = 10 }
			add_to_variable = { BOS_support = 4 }
			subtract_from_variable = { HUN_support = 8 }
			every_owned_state = {
				limit = {
					is_core_of = CRO
					is_controlled_by = AUH
				}
				add_compliance = 6
			}
			# the forgeries were read out in Vienna
			if = {
				limit = { country_exists = SER }
				diplomatic_relation = {
					country = SER
					relation = non_aggression_pact
					active = yes
				}
			}
			custom_effect_tooltip = AUH_settlement_zagreb_reversed_tt
			AUH_settlement_bound_the_registers = yes
		}

		ai_will_do = {
			factor = 55
		}
	}
}

AUH_settlement_rescue = {

	# drafted at the Belvedere, never granted
	AUH_settlement_third_crown = {

		icon = GFX_decision_generic_form_nation
		priority = 70
		cost = 175
		days_remove = 0
		fire_only_once = yes

		allowed = {
			original_tag = AUH
		}

		visible = {
			tag = AUH
			NOT = { has_country_flag = AUH_settlement_charter_signed }
			NOT = { has_country_flag = AUH_third_crown_granted }
		}

		available = {
			NOT = { has_country_flag = AUH_settlement_magyar_charter }
			NOT = { has_country_flag = AUH_settlement_polish_charter }
			NOT = { has_country_flag = AUH_settlement_federal_charter }
			NOT = { has_country_flag = AUH_settlement_register_charter }
			custom_trigger_tooltip = {
				OR = {
					check_variable = { CRO_support < 35 }
					check_variable = { SLV_support < 35 }
					check_variable = { SER_support < 35 }
				}
				tooltip = AUH_settlement_south_slav_lost_tt
			}
			check_variable = { HUN_support > 29 }
		}

		complete_effect = {
			set_country_flag = AUH_settlement_trialism_charter
			set_country_flag = AUH_third_crown_granted
			AUH_settlement_seal_the_charter = yes
			add_ideas = AUH_settlement_the_third_crown_charter
			add_to_variable = { CRO_support = 25 }
			add_to_variable = { SLV_support = 20 }
			add_to_variable = { SER_support = 15 }
			add_to_variable = { BOS_support = 12 }
			subtract_from_variable = { HUN_support = 22 }
			subtract_from_variable = { AUH_gemeinsamkeit = 15 }
			subtract_from_variable = { AUH_quote = 3 }
			# Saint Stephen loses Croatia
			if = {
				limit = { NOT = { has_idea = AUH_settlement_the_counties_refuse } }
				add_timed_idea = {
					idea = AUH_settlement_the_counties_refuse
					days = 540
				}
			}
			if = {
				limit = { NOT = { country_exists = CRO } }
				release_autonomy = {
					target = CRO
					autonomy_state = autonomy_integrated_puppet
				}
			}
			custom_effect_tooltip = AUH_settlement_third_crown_tt
			hidden_effect = {
				set_variable = { AUH_news_id = 607 }
				set_variable = { AUH_news_scope = 3 }
				AUH_headline = yes
			}
			AUH_settlement_bound_the_registers = yes
		}

		ai_will_do = {
			factor = 30
		}
	}

	# Kristoffy proposed it, nobody signed it
	AUH_settlement_hungarian_franchise_octroi = {

		icon = GFX_decision_generic_election
		priority = 69
		cost = 200
		days_remove = 0
		fire_only_once = yes

		allowed = {
			original_tag = AUH
		}

		visible = {
			tag = AUH
			NOT = { has_country_flag = AUH_settlement_charter_signed }
		}

		available = {
			NOT = { has_country_flag = AUH_settlement_trialism_charter }
			NOT = { has_country_flag = AUH_settlement_polish_charter }
			NOT = { has_country_flag = AUH_settlement_federal_charter }
			NOT = { has_country_flag = AUH_settlement_register_charter }
			custom_trigger_tooltip = {
				OR = {
					check_variable = { ROM_support < 35 }
					check_variable = { SLO_support < 35 }
					check_variable = { SER_support < 35 }
				}
				tooltip = AUH_settlement_hungarys_nations_lost_tt
			}
			has_army_experience > 9
		}

		complete_effect = {
			set_country_flag = AUH_settlement_magyar_charter
			AUH_settlement_seal_the_charter = yes
			add_ideas = AUH_settlement_the_magyar_franchise
			add_ideas = AUH_settlement_the_counties_refuse
			add_to_variable = { ROM_support = 22 }
			add_to_variable = { SLO_support = 22 }
			add_to_variable = { SER_support = 15 }
			add_to_variable = { CRO_support = 10 }
			subtract_from_variable = { HUN_support = 28 }
			subtract_from_variable = { AUH_gemeinsamkeit = 10 }
			add_popularity = {
				ideology = democratic
				popularity = 0.08
			}
			add_war_support = -0.05
			# Fall U was in the drawer
			army_experience = 25
			every_owned_state = {
				limit = {
					is_core_of = HUN
					is_controlled_by = AUH
				}
				add_resistance = 10
			}
			custom_effect_tooltip = AUH_settlement_magyar_franchise_tt
			hidden_effect = {
				set_variable = { AUH_news_id = 604 }
				set_variable = { AUH_news_scope = 3 }
				AUH_headline = yes
			}
			AUH_settlement_bound_the_registers = yes
		}

		ai_will_do = {
			factor = 20
		}
	}

	# Galicia raised out of the provinces
	AUH_settlement_austro_polish_crown = {

		icon = GFX_decision_generic_form_nation
		priority = 68
		cost = 175
		days_remove = 0
		fire_only_once = yes

		allowed = {
			original_tag = AUH
		}

		visible = {
			tag = AUH
			NOT = { has_country_flag = AUH_settlement_charter_signed }
		}

		available = {
			NOT = { has_country_flag = AUH_settlement_trialism_charter }
			NOT = { has_country_flag = AUH_settlement_magyar_charter }
			NOT = { has_country_flag = AUH_settlement_federal_charter }
			NOT = { has_country_flag = AUH_settlement_register_charter }
			custom_trigger_tooltip = {
				check_variable = { POL_support < 35 }
				tooltip = AUH_settlement_pole_lost_tt
			}
			check_variable = { UKR_support > 19 }
		}

		complete_effect = {
			set_country_flag = AUH_settlement_polish_charter
			AUH_settlement_seal_the_charter = yes
			add_ideas = AUH_settlement_the_austro_polish_crown
			add_to_variable = { POL_support = 30 }
			subtract_from_variable = { UKR_support = 20 }
			subtract_from_variable = { HUN_support = 8 }
			subtract_from_variable = { AUH_gemeinsamkeit = 8 }
			add_manpower = 15000
			every_owned_state = {
				limit = {
					is_core_of = POL
					is_controlled_by = AUH
				}
				add_compliance = 10
			}
			every_owned_state = {
				limit = {
					is_core_of = UKR
					is_controlled_by = AUH
				}
				add_resistance = 12
			}
			# Berlin has a Polish question of its own
			if = {
				limit = { country_exists = GER }
				add_named_threat = {
					threat = 2
					name = AUH_settlement_polish_crown_threat
				}
			}
			custom_effect_tooltip = AUH_settlement_austro_polish_crown_tt
			hidden_effect = {
				set_variable = { AUH_news_id = 608 }
				set_variable = { AUH_news_scope = 3 }
				AUH_headline = yes
			}
			AUH_settlement_bound_the_registers = yes
		}

		ai_will_do = {
			factor = 25
		}
	}

	# Popovici, Leipzig 1906
	AUH_settlement_fifteen_states = {

		icon = GFX_decision_generic_monarchy
		priority = 67
		cost = 200
		days_remove = 0
		fire_only_once = yes

		allowed = {
			original_tag = AUH
		}

		visible = {
			tag = AUH
			NOT = { has_country_flag = AUH_settlement_charter_signed }
		}

		available = {
			NOT = { has_country_flag = AUH_settlement_trialism_charter }
			NOT = { has_country_flag = AUH_settlement_magyar_charter }
			NOT = { has_country_flag = AUH_settlement_polish_charter }
			NOT = { has_country_flag = AUH_settlement_register_charter }
			date > 1906.6.1
			custom_trigger_tooltip = {
				OR = {
					check_variable = { CZE_support < 35 }
					check_variable = { ROM_support < 35 }
					check_variable = { CRO_support < 35 }
					check_variable = { UKR_support < 35 }
				}
				tooltip = AUH_settlement_the_book_is_read_tt
			}
			has_stability > 0.30
		}

		complete_effect = {
			set_country_flag = AUH_settlement_federal_charter
			AUH_settlement_seal_the_charter = yes
			add_ideas = AUH_settlement_the_fifteen_states_charter
			add_to_variable = { CZE_support = 18 }
			add_to_variable = { SLO_support = 18 }
			add_to_variable = { ROM_support = 18 }
			add_to_variable = { CRO_support = 18 }
			add_to_variable = { SLV_support = 18 }
			add_to_variable = { UKR_support = 18 }
			add_to_variable = { SER_support = 12 }
			add_to_variable = { BOS_support = 12 }
			add_to_variable = { ITA_support = 12 }
			add_to_variable = { POL_support = 8 }
			subtract_from_variable = { AUS_support = 22 }
			subtract_from_variable = { HUN_support = 30 }
			subtract_from_variable = { AUH_gemeinsamkeit = 25 }
			add_stability = -0.10
			add_research_slot = 1
			# an octroi is proclaimed, not voted
			add_ideas = AUH_settlement_the_counties_refuse
			custom_effect_tooltip = AUH_settlement_fifteen_states_tt
			hidden_effect = {
				set_variable = { AUH_news_id = 609 }
				set_variable = { AUH_news_scope = 3 }
				AUH_headline = yes
			}
			AUH_settlement_bound_the_registers = yes
		}

		ai_will_do = {
			factor = 15
		}
	}

	# the Moravian principle made imperial
	AUH_settlement_personal_register = {

		icon = GFX_decision_generic_book
		priority = 66
		cost = 160
		days_remove = 0
		fire_only_once = yes

		allowed = {
			original_tag = AUH
		}

		visible = {
			tag = AUH
			NOT = { has_country_flag = AUH_settlement_charter_signed }
		}

		available = {
			NOT = { has_country_flag = AUH_settlement_trialism_charter }
			NOT = { has_country_flag = AUH_settlement_magyar_charter }
			NOT = { has_country_flag = AUH_settlement_polish_charter }
			NOT = { has_country_flag = AUH_settlement_federal_charter }
			# untested in a province, useless in an empire
			OR = {
				has_idea = AUH_settlement_the_moravian_cadastre
				has_idea = AUH_settlement_the_bucovina_seats
				has_idea = AUH_settlement_the_galician_seats
			}
			custom_trigger_tooltip = {
				OR = {
					check_variable = { UKR_support < 35 }
					check_variable = { SLO_support < 35 }
					check_variable = { SLV_support < 35 }
					check_variable = { ITA_support < 35 }
				}
				tooltip = AUH_settlement_small_nations_lost_tt
			}
		}

		complete_effect = {
			set_country_flag = AUH_settlement_register_charter
			AUH_settlement_seal_the_charter = yes
			add_ideas = AUH_settlement_the_personal_register
			add_to_variable = { UKR_support = 16 }
			add_to_variable = { SLO_support = 16 }
			add_to_variable = { SLV_support = 16 }
			add_to_variable = { ITA_support = 14 }
			add_to_variable = { ROM_support = 10 }
			add_to_variable = { BOS_support = 10 }
			subtract_from_variable = { AUS_support = 14 }
			subtract_from_variable = { HUN_support = 14 }
			subtract_from_variable = { AUH_gemeinsamkeit = 6 }
			add_stability = 0.05
			# an open register does not close again
			custom_effect_tooltip = AUH_settlement_personal_register_tt
			AUH_settlement_bound_the_registers = yes
		}

		ai_will_do = {
			factor = 25
		}
	}
}

AUH_settlement_emergency = {

	# sent where the gendarmerie cannot go
	AUH_settlement_commission_of_inquiry = {

		icon = GFX_decision_generic_civil_support
		priority = 60
		cost = 60
		days_remove = 60
		fire_only_once = no

		allowed = {
			original_tag = AUH
		}

		visible = {
			tag = AUH
		}

		available = {
			custom_trigger_tooltip = {
				OR = {
					has_active_mission = serbian_uprising
					has_active_mission = croatian_uprising
					has_active_mission = bosnian_uprising
					has_active_mission = slovenian_uprising
					has_active_mission = austrian_uprising
					has_active_mission = CZE_uprising
					has_active_mission = HUN_uprising
					has_active_mission = ROM_uprising
					has_active_mission = POL_uprising
					has_active_mission = UKR_uprising
				}
				tooltip = AUH_settlement_a_province_in_arms_tt
			}
		}

		complete_effect = {
			custom_effect_tooltip = AUH_settlement_commission_tt
			AUH_settlement_adjourn_the_risings = yes
		}

		ai_will_do = {
			factor = 150
		}
	}

	# Prague 1908, Croatia 1912
	AUH_settlement_state_of_siege = {

		icon = GFX_decision_generic_arrest
		priority = 59
		cost = 100
		days_remove = 0
		fire_only_once = no

		allowed = {
			original_tag = AUH
		}

		visible = {
			tag = AUH
		}

		available = {
			custom_trigger_tooltip = {
				OR = {
					has_active_mission = serbian_uprising
					has_active_mission = croatian_uprising
					has_active_mission = bosnian_uprising
					has_active_mission = slovenian_uprising
					has_active_mission = austrian_uprising
					has_active_mission = CZE_uprising
					has_active_mission = HUN_uprising
					has_active_mission = ROM_uprising
					has_active_mission = POL_uprising
					has_active_mission = UKR_uprising
				}
				tooltip = AUH_settlement_a_province_in_arms_tt
			}
			has_army_experience > 4
		}

		complete_effect = {
			add_timed_idea = {
				idea = AUH_settlement_government_by_commissioner
				days = 730
			}
			AUH_settlement_disperse_the_risings = yes
			add_stability = 0.05
			army_experience = -5
			# read in every other crown land
			subtract_from_variable = { CZE_support = 3 }
			subtract_from_variable = { SLO_support = 3 }
			subtract_from_variable = { SLV_support = 3 }
			subtract_from_variable = { CRO_support = 3 }
			subtract_from_variable = { UKR_support = 3 }
			subtract_from_variable = { ITA_support = 3 }
			subtract_from_variable = { AUH_gemeinsamkeit = 4 }
			custom_effect_tooltip = AUH_settlement_state_of_siege_tt
			hidden_effect = {
				set_variable = { AUH_news_id = 610 }
				set_variable = { AUH_news_scope = 2 }
				AUH_headline = yes
			}
			AUH_settlement_bound_the_registers = yes
		}

		ai_will_do = {
			factor = 120
		}
	}

	# signed with a rifle on the table
	AUH_settlement_charter_under_duress = {

		icon = GFX_decision_generic_form_nation
		priority = 58
		cost = 150
		days_remove = 0
		fire_only_once = no

		allowed = {
			original_tag = AUH
		}

		visible = {
			tag = AUH
		}

		available = {
			custom_trigger_tooltip = {
				OR = {
					has_active_mission = serbian_uprising
					has_active_mission = croatian_uprising
					has_active_mission = bosnian_uprising
					has_active_mission = slovenian_uprising
					has_active_mission = austrian_uprising
					has_active_mission = CZE_uprising
					has_active_mission = HUN_uprising
					has_active_mission = ROM_uprising
					has_active_mission = POL_uprising
					has_active_mission = UKR_uprising
				}
				tooltip = AUH_settlement_a_province_in_arms_tt
			}
			has_stability > 0.20
		}

		complete_effect = {
			if = {
				limit = { NOT = { has_idea = AUH_settlement_the_charter_under_duress } }
				add_ideas = AUH_settlement_the_charter_under_duress
			}
			AUH_settlement_charter_the_risings = yes
			subtract_from_variable = { AUS_support = 8 }
			subtract_from_variable = { HUN_support = 8 }
			subtract_from_variable = { AUH_gemeinsamkeit = 8 }
			add_war_support = -0.05
			custom_effect_tooltip = AUH_settlement_charter_under_duress_tt
			AUH_settlement_bound_the_registers = yes
		}

		ai_will_do = {
			factor = 80
		}
	}

	# release without the annexation
	AUH_settlement_subjects_not_rebels = {

		icon = GFX_decision_generic_independence
		priority = 57
		cost = 175
		days_remove = 0
		fire_only_once = no

		allowed = {
			original_tag = AUH
		}

		visible = {
			tag = AUH
		}

		available = {
			custom_trigger_tooltip = {
				OR = {
					has_active_mission = croatian_uprising
					has_active_mission = bosnian_uprising
					has_active_mission = slovenian_uprising
					has_active_mission = CZE_uprising
					has_active_mission = HUN_uprising
					has_active_mission = ROM_uprising
					has_active_mission = POL_uprising
					has_active_mission = UKR_uprising
				}
				tooltip = AUH_settlement_a_province_to_release_tt
			}
			has_political_power > 174
		}

		complete_effect = {
			if = {
				limit = { NOT = { has_idea = AUH_settlement_the_subjects_of_the_crown } }
				add_ideas = AUH_settlement_the_subjects_of_the_crown
			}
			AUH_settlement_subject_the_risings = yes
			subtract_from_variable = { AUS_support = 10 }
			subtract_from_variable = { AUH_gemeinsamkeit = 20 }
			add_war_support = -0.08
			custom_effect_tooltip = AUH_settlement_subjects_not_rebels_tt
			hidden_effect = {
				set_variable = { AUH_news_id = 611 }
				set_variable = { AUH_news_scope = 3 }
				AUH_headline = yes
			}
			AUH_settlement_bound_the_registers = yes
		}

		ai_will_do = {
			factor = 60
		}
	}
}
