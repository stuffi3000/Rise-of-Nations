#Category only allowed for GER
#Category only allowed for GER
WW2_operations_decisions = {

	PST_annex_vatican_city = {
		icon = generic_army_support
		allowed = {
			tag = PST
		}

		visible = {
			VCT = { exists = yes }
		}

		cost = 0
		days_remove = 10

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				#Not worth the risk if We are already at War with a major
				any_country = {
					is_major = yes
					has_war_with = ROOT
				}
			}
			modifier = {
				#Not worth the risk if we are losing the war
				ROOT = {
					surrender_progress > 0.05
				}
				factor = 0
			}
			modifier = {
				#Need a land link
				NOT = {
					is_neighbor_of = VCT
				}
				factor = 0
			}
		}
		remove_effect = {
			if = {
				limit = {
					VCT = { 
						is_ai = no 
					}
				}
				create_wargoal = {
				type = annex_everything
				target = PST
				}
			}
			
			if = {
				limit = {
					VCT = { 
						is_ai = yes
					}
				}
				annex_country = {
				target = VCT
				transfer_troops = yes
				}
			}
			
		}

	}

	BS_invade_monaco = {
		icon = generic_army_support
		allowed = {

		}

		visible = {
			NOT = { has_government = democratic }
			has_war = yes
			controls_state = 821
			MCO = { controls_state = 825 }

		}

		highlight_states = {
			state = 825
		}

		cost = 40
		days_remove = 10

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				#Not worth the risk if We are already at War with a major
				NOT = {
					any_country = {
						is_major = yes
						is_in_faction_with = ROOT
					}
				}
				any_country = {
					is_major = yes
					has_war_with = ROOT
				}
			}
			modifier = {
				#Not worth the risk if we are losing the war
				ROOT = {
					surrender_progress > 0.05
				}
				factor = 0
			}
			modifier = {
				#Need a land link
				NOT = {
					is_neighbor_of = MCO
				}
				factor = 0
			}
		}
		remove_effect = {
			create_wargoal = {
				type = annex_everything
				target = MCO
			}
		}

	}

	BS_invade_liechtenstein = {
		icon = generic_army_support
		allowed = {

		}

		visible = {
			NOT = { has_government = democratic }
			has_war = yes
			controls_state = 151
			LIE = { controls_state = 826 }

		}

		highlight_states = {
			state = 826
		}

		cost = 40
		days_remove = 10

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				#Not worth the risk if We are already at War with a major
				NOT = {
					any_country = {
						is_major = yes
						is_in_faction_with = ROOT
					}
				}
				any_country = {
					is_major = yes
					has_war_with = ROOT
				}
			}
			modifier = {
				#Not worth the risk if we are losing the war
				ROOT = {
					surrender_progress > 0.05
				}
				factor = 0
			}
			modifier = {
				#Need a land link
				NOT = {
					is_neighbor_of = LIE
				}
				factor = 0
			}
		}
		remove_effect = {
			create_wargoal = {
				type = annex_everything
				target = LIE
			}
		}

	}

	GER_operation_weserubung = {
		icon = generic_army_support

		available = { 
			is_ai = yes
			tag = GER
			date > 1939.1.1
			date < 1941.1.1
			has_war_with = ENG
			OR = {
				has_war_with = DEN
				has_war_with = NOR
			}
		}

		cost = 0

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				OR = {
					DEN = { has_capitulated = no }
					DEN = { exists = yes }
					NOT = { DEN = { is_subject_of = GER } }
					NOT = { RPD = { is_subject_of = GER } }
				}
			}
			modifier = {
				factor = 0
				NOT = { has_war_with = NOR }
			}
		}
		
		days_remove = 61

		visible = {
			is_ai = yes
			tag = GER
			NOT = { has_country_flag = ww2_operation }
		}

		complete_effect = {
			set_country_flag = ww2_operation
			add_timed_idea = {
				idea = blitzkrieg_1940
				days = 61
			}
			add_timed_idea = {
				idea = luftwaffe_1940
				days = 61
			}
			load_oob = GER_WESERUBUNG
		}
		
		remove_effect = {
			clr_country_flag = ww2_operation
		}
	}

	GER_fall_gelb = {
		icon = generic_army_support

		available = { 
			is_ai = yes
			tag = GER
			date > 1939.1.1
			date < 1941.1.1
			OR = {
				has_war_with = ENG
				has_war_with = FRA
			}
			OR = {
				has_war_with = HOL
				has_war_with = BEL
				has_war_with = LUX
			}


		}

		cost = 0

		fire_only_once = yes

		visible = {
			is_ai = yes
			tag = GER
			NOT = { has_country_flag = ww2_operation }
		}

		ai_will_do = {
			factor = 1000
		}
		
		days_remove = 46

		complete_effect = {
			set_country_flag = ww2_operation
			add_timed_idea = {
				idea = blitzkrieg_1940
				days = 46
			}
			add_timed_idea = {
				idea = luftwaffe_1940
				days = 46
			}
		}
		
		remove_effect = {
			clr_country_flag = ww2_operation
		}
	}



	GER_operation_sealion = {
		icon = generic_army_support

		available = { 
			is_ai = yes
			tag = GER
			date > 1940.1.1
			date < 1950.1.1

			has_war_with = ENG
			
			RON_POL_Defeated = yes
			RON_FRA_Defeated = yes
			RON_SOV_Defeated = yes


		}

		visible = {
			is_ai = yes
			tag = GER
			NOT = { has_country_flag = ww2_operation }
		}

		cost = 0

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				has_war_with = SOV
			}
			modifier = {
				factor = 0
				date < 1943.1.1
			}
		}

		days_remove = 84

		complete_effect = {
			set_country_flag = ww2_operation
			add_timed_idea = {
				idea = operation_sealion
				days = 84
			}
			country_event = {
				hours = 6
				id = ww2operations.9
			}
			load_oob = GER_SEALION
		}
		
		remove_effect = {
			clr_country_flag = ww2_operation
		}
	}

	GER_break_non_aggression_pact_with_ussr = {
		icon = generic_army_support

		available = {
			tag = GER
			date > 1941.1.1
			has_completed_focus = GER_soviet_invasion
			has_non_aggression_pact_with = SOV

			NOT = {
				has_war_with = SOV
			}

		}

		visible = {
			tag = GER
			NOT = { has_country_flag = ww2_operation }
		}

		cost = 0

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				date < 1941.6.1
			}
		}

		complete_effect = {
			diplomatic_relation = { country = SOV relation = non_aggression_pact active = no }
		}
	}

	GER_operation_barbarossa = {
		icon = generic_army_support

		available = {
			#is_ai = yes
			tag = GER
			date > 1941.1.1
			date < 1941.7.1
			has_completed_focus = GER_soviet_invasion
			has_war_with = SOV
		}

		visible = {
			tag = GER
			NOT = { has_country_flag = ww2_operation }
		}

		cost = 0

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				date < 1941.5.1
			}
		}

		days_remove = 210

		complete_effect = {
			set_country_flag = ww2_operation
			activate_mission = GER_barbarossa_mission
			country_event = { id = ww2operations.14 }
			
			add_timed_idea = {
				idea = blitzkrieg_1941
				days = 160
			}
			add_timed_idea = {
				idea = luftwaffe_1941
				days = 210
			}
			
			SOV = {
				add_timed_idea = {
					idea = shattered_retreat
					days = 160
				}
				add_timed_idea = {
					idea = soviet_air_forces_1941
					days = 129
				}
				add_timed_idea = {
					idea = army_destroyed
					days = 49
				}
			}
		}
		
		remove_effect = {
			clr_country_flag = ww2_operation
		}
	}

	GER_operation_typhoon = {
		icon = generic_army_support

		available = {
			#is_ai = yes
			tag = GER
			date > 1941.8.1
			date < 1941.12.15
			has_war_with = SOV
			242 = { is_controlled_by_ROOT_or_ally = yes }
		}

		visible = {
			OR = {
				has_idea = blitzkrieg_1941
				has_idea = luftwaffe_1941
			}
		}

		cost = 0

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				date < 1941.10.1
			}
		}
		
		days_remove = 95

		complete_effect = {
			set_country_flag = ww2_operation
			remove_ideas = blitzkrieg_1941
			add_timed_idea = {
				idea = operation_typhoon
				days = 95
			}
			country_event = { id = ww2operations.11 }
		}
		
		remove_effect = {
			clr_country_flag = ww2_operation
		}
	}

	GER_fall_blau = {
		icon = generic_army_support

		available = {
			#is_ai = yes
			tag = GER
			date > 1942.1.1
			date < 1942.8.1
			has_war_with = SOV
			226 = { is_controlled_by_ROOT_or_ally = yes }
		}

		visible = {
			tag = GER
			NOT = { has_country_flag = ww2_operation }
		}

		cost = 0

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				date < 1942.7.1
			}
		}

		days_remove = 143

		complete_effect = {
			set_country_flag = ww2_operation
			add_timed_idea = {
				idea = fall_blau
				days = 143
			}
			country_event = { id = ww2operations.2 }
		}
		
		remove_effect = {
			clr_country_flag = ww2_operation
		}
	}

	GER_operation_citadel = {
		icon = generic_army_support

		available = {
			#is_ai = yes
			tag = GER
			date > 1943.1.1
			date < 1943.8.1
			has_war_with = SOV
			225 = { is_controlled_by_ROOT_or_ally = yes }
		}

		visible = {
			tag = GER
			NOT = { has_country_flag = ww2_operation }
		}

		cost = 0

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				date < 1943.6.1
			}
		}

		days_remove = 12

		complete_effect = {
			set_country_flag = ww2_operation
			add_timed_idea = {
				idea = operation_citadel
				days = 12
			}
			SOV = {
				add_timed_idea = {
					idea = SOV_operation_citadel
					days = 12
				}
			}
			country_event = { id = ww2operations.12 }
		}
		
		remove_effect = {
			clr_country_flag = ww2_operation
		}
	}

	GER_operation_watch = {
		icon = generic_army_support

		available = {
			#is_ai = yes
			tag = GER
			date > 1944.1.1
			date < 1950.1.1
			6 = { is_controlled_by_ROOT_or_ally = no }
			OR = {
				has_war_with = USA
				has_war_with = ENG
			}
		}

		visible = {
			tag = GER
			NOT = { has_country_flag = ww2_operation }
		}

		cost = 0

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				date < 1944.12.1
			}
		}

		days_remove = 42

		complete_effect = {
			set_country_flag = ww2_operation
			add_timed_idea = {
				idea = battle_of_the_bulge
				days = 42
			}
			USA = {
				add_timed_idea = {
					idea = grounded_usaaf_1944
					days = 21
				}
				add_timed_idea = {
					idea = army_destroyed
					days = 14
				}
			}
			ENG = {
				add_timed_idea = {
					idea = grounded_usaaf_1944
					days = 21
				}
				add_timed_idea = {
					idea = army_destroyed
					days = 14
				}
			}
			country_event = { id = ww2operations.3 }
		}
		
		remove_effect = {
			clr_country_flag = ww2_operation
		}
	}

	SOV_winter_campaign = {
		icon = generic_army_support

		available = {
			#is_ai = yes
			tag = SOV
			date > 1941.12.1
			date < 1942.5.1
			has_war_with = GER

			surrender_progress > 0.01

			OR = {
				242 = { is_controlled_by_ROOT_or_ally = no }
				208 = { is_controlled_by_ROOT_or_ally = no }
				202 = { is_controlled_by_ROOT_or_ally = no }
			}

		}

		visible = {
			tag = SOV
			NOT = { has_country_flag = ww2_operation }
		}

		cost = 0

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
		}

		days_remove = 150

		complete_effect = {
			set_country_flag = ww2_operation
			add_timed_idea = {
				idea = sov_soviet_winter
				days = 150
			}
			country_event = { id = ww2operations.24 }
		}
		
		remove_effect = {
			clr_country_flag = ww2_operation
		}
	}

	SOV_operation_uranus = {
		icon = generic_army_support

		available = {
			#is_ai = yes
			tag = SOV
			date > 1942.6.1
			date < 1943.1.1
			has_war_with = GER
			217 = { is_controlled_by_ROOT_or_ally = yes }
			surrender_progress > 0.01
		}

		visible = {
			tag = SOV
			NOT = { has_country_flag = ww2_operation }
		}

		cost = 0

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				date < 1942.11.1
			}
		}

		days_remove = 4

		complete_effect = {
			set_country_flag = ww2_operation
			add_timed_idea = {
				idea = soviet_steamroller
				days = 4
			}
			GER = {
				add_timed_idea = {
					idea = army_destroyed
					days = 4
				}
			}
			country_event = { id = ww2operations.23 }
		}
		
		remove_effect = {
			clr_country_flag = ww2_operation
		}
	}

	SOV_operation_mars = {
		icon = generic_army_support

		available = {
			#is_ai = yes
			tag = SOV
			date > 1942.6.1
			date < 1943.1.1
			has_war_with = GER
			219 = { is_controlled_by_ROOT_or_ally = yes }
			surrender_progress > 0.01
		}

		visible = {
			tag = SOV
			NOT = { has_country_flag = ww2_operation }
		}

		cost = 0

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				date < 1942.11.25
			}
		}

		days_remove = 25

		complete_effect = {
			set_country_flag = ww2_operation
			add_timed_idea = {
				idea = soviet_steamroller
				days = 25
			}
			GER = {
				add_timed_idea = {
					idea = army_destroyed
					days = 8
				}
			}
			country_event = { id = ww2operations.22 }
		}
		
		remove_effect = {
			clr_country_flag = ww2_operation
		}
	}

	SOV_battle_of_dnieper = {
		icon = generic_army_support

		available = {
			#is_ai = yes
			tag = SOV
			date > 1943.6.1
			date < 1944.1.1
			has_war_with = GER
			surrender_progress > 0.01
			OR = {
				220 = { is_controlled_by_ROOT_or_ally = yes }
				264 = { is_controlled_by_ROOT_or_ally = yes }
			}
		}

		visible = {
			tag = SOV
			NOT = { has_country_flag = ww2_operation }
		}

		cost = 0

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				date < 1943.8.24
			}
		}

		days_remove = 117

		complete_effect = {
			set_country_flag = ww2_operation
			add_timed_idea = {
				idea = soviet_steamroller
				days = 117
			}
			country_event = { id = ww2operations.20 }
		}
		
		remove_effect = {
			clr_country_flag = ww2_operation
		}
	}

	SOV_dnieper_offensive = {
		icon = generic_army_support

		available = {
			#is_ai = yes
			tag = SOV
			date > 1943.11.1
			date < 1944.2.1
			has_war_with = GER
			surrender_progress > 0.01

			OR = {
				195 = { is_controlled_by_ROOT_or_ally = yes }
				202 = { is_controlled_by_ROOT_or_ally = yes }
				207 = { is_controlled_by_ROOT_or_ally = yes }
			}

		}

		visible = {
			tag = SOV
			NOT = { has_country_flag = ww2_operation }
		}

		cost = 0

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				date < 1943.12.24
			}
		}

		days_remove = 113

		complete_effect = {
			set_country_flag = ww2_operation
			add_timed_idea = {
				idea = soviet_steamroller
				days = 113
			}
			country_event = { id = ww2operations.21 }
		}
		
		remove_effect = {
			clr_country_flag = ww2_operation
		}
	}

	SOV_operation_bagration = {
		icon = generic_army_support

		available = {
			#is_ai = yes
			tag = SOV
			date > 1944.1.1
			date < 1945.1.1
			has_war_with = GER
			surrender_progress > 0.01

			OR = {
				208 = { is_controlled_by_ROOT_or_ally = yes }
				201 = { is_controlled_by_ROOT_or_ally = yes }
				192 = { is_controlled_by_ROOT_or_ally = yes }
			}

		}

		visible = {
			tag = SOV
			NOT = { has_country_flag = ww2_operation }
		}

		cost = 0

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				date < 1944.6.22
			}
		}

		days_remove = 56

		complete_effect = {
			set_country_flag = ww2_operation
			add_timed_idea = {
				idea = soviet_steamroller
				days = 56
			}
			GER = {
				add_timed_idea = {
					idea = army_destroyed
					days = 18
				}
			}
			country_event = { id = ww2operations.8 }
		}
		
		remove_effect = {
			clr_country_flag = ww2_operation
		}
	}

	SOV_vistula_offensive = {
		icon = generic_army_support

		available = { #is_ai = yes 
			tag = SOV
			date > 1945.1.1
			date < 1950.1.1
			has_war_with = GER

			OR = {
				11 = { is_controlled_by_ROOT_or_ally = yes }
				94 = { is_controlled_by_ROOT_or_ally = yes }
				43 = { is_controlled_by_ROOT_or_ally = yes }
			}

		}

		visible = {
			tag = SOV
			NOT = { has_country_flag = ww2_operation }
		}

		cost = 0

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				date < 1945.1.12
			}
		}

		days_remove = 20

		complete_effect = {
			set_country_flag = ww2_operation
			add_timed_idea = {
				idea = soviet_steamroller
				days = 20
			}
			GER = {
				add_timed_idea = {
					idea = army_destroyed
					days = 7
				}
			}
			country_event = { id = ww2operations.19 }
		}
		
		remove_effect = {
			clr_country_flag = ww2_operation
		}
	}

	SOV_vienna_offensive = {
		icon = generic_army_support

		available = { #is_ai = yes 
			tag = SOV
			date > 1945.1.1
			date < 1950.1.1
			has_war_with = GER
			surrender_progress < 0.1

			OR = {
				70 = { is_controlled_by_ROOT_or_ally = yes }
				43 = { is_controlled_by_ROOT_or_ally = yes }
			}

		}

		visible = {
			tag = SOV
			NOT = { has_country_flag = ww2_operation }
		}

		cost = 0

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				date < 1945.3.16
			}
		}

		days_remove = 11

		complete_effect = {
			set_country_flag = ww2_operation
			add_timed_idea = {
				idea = soviet_steamroller
				days = 11
			}
			GER = {
				add_timed_idea = {
					idea = army_destroyed
					days = 4
				}
			}
			country_event = { id = ww2operations.18 }
		}
		
		remove_effect = {
			clr_country_flag = ww2_operation
		}
	}

	SOV_operation_august_storm = {
		icon = generic_army_support

		available = { #is_ai = yes 
			tag = SOV
			date > 1945.1.1
			date < 1950.1.1
			has_war_with = JAP
			surrender_progress < 0.1
		}

		visible = {
			tag = SOV
			NOT = { has_country_flag = ww2_operation }
		}

		cost = 0

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				date < 1945.8.9
			}
		}

		days_remove = 11

		complete_effect = {
			set_country_flag = ww2_operation
			add_timed_idea = {
				idea = soviet_steamroller
				days = 11
			}
			JAP = {
				add_timed_idea = {
					idea = army_destroyed
					days = 4
				}
			}
			country_event = { id = ww2operations.16 }
		}
		
		remove_effect = {
			clr_country_flag = ww2_operation
		}
	}

	USA_operation_torch = {
		icon = generic_army_support

		available = { 
			is_ai = yes
			tag = USA
			date > 1942.1.1
			date < 1943.1.1
			surrender_progress < 0.1
			OR = {
				has_war_with = GER
				has_war_with = ITA
			}
			OR = {
				459 = { is_controlled_by_ROOT_or_ally = no }
				461 = { is_controlled_by_ROOT_or_ally = no }
			}
		}

		visible = {
			is_ai = yes
			tag = USA
			NOT = { has_country_flag = ww2_operation }
		}

		cost = 0

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				date < 1942.11.8
			}
		}

		days_remove = 1

		complete_effect = {
			set_country_flag = ww2_operation
			USA = {
				load_oob = USA_OPERATION_TORCH
			}
			ENG = {
				load_oob = ENG_OPERATION_TORCH
			}
		}
		
		remove_effect = {
			clr_country_flag = ww2_operation
		}
	}

	USA_operation_husky = {
		icon = generic_army_support

		available = { 
			is_ai = yes
			tag = USA
			date > 1943.1.1
			date < 1944.1.1
			has_start_date < 1943.1.1
			surrender_progress < 0.1
			OR = {
				has_war_with = GER
				has_war_with = ITA
			}
			458 = { is_controlled_by_ROOT_or_ally = yes }
			459 = { is_controlled_by_ROOT_or_ally = yes }
			461 = { is_controlled_by_ROOT_or_ally = yes }
		}

		visible = {
			is_ai = yes
			tag = USA
			NOT = { has_country_flag = ww2_operation }
		}

		cost = 0

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				date < 1943.7.9
			}
		}


		days_remove = 1

		complete_effect = {
			set_country_flag = ww2_operation
			USA = {
				load_oob = USA_OPERATION_HUSKY
			}
			ENG = {
				load_oob = ENG_OPERATION_HUSKY
			}
			ITA = {
				add_timed_idea = {
					idea = army_destroyed
					days = 12
				}
			}
		}
		
		remove_effect = {
			clr_country_flag = ww2_operation
		}
	}

	USA_operation_overlord = {
			icon = generic_army_support
	
			available = { 
			is_ai = yes
			tag = USA
			date > 1944.1.1
			date < 1950.1.1
			has_war_with = GER
			surrender_progress < 0.1
			126 = { is_controlled_by_ROOT_or_ally = yes }
			15 = { is_controlled_by_ROOT_or_ally = no }
			29 = { is_controlled_by_ROOT_or_ally = no }
			785 = { is_controlled_by_ROOT_or_ally = no }
			}
	
			visible = {
			is_ai = yes
			tag = USA
			NOT = { has_country_flag = ww2_operation }
			}
	
			cost = 0
	
			fire_only_once = yes
	
			ai_will_do = {
				factor = 1000
				modifier = {
					factor = 0
					date < 1944.6.6
				}
			}
	
			days_remove = 84

		complete_effect = {
			set_country_flag = ww2_operation
			GER = {
				add_timed_idea = {
					idea = grounded_luftwaffe_1944
					days = 84
				}
				add_timed_idea = {
					idea = army_destroyed
					days = 26
				}
				country_event = {
					id = ww2operations.15
				}
			}
			USA = {
				add_timed_idea = {
					idea = operation_overlord
					days = 84
				}
				load_oob = USA_DDAY
			}
			ENG = {
				add_timed_idea = {
					idea = operation_overlord
					days = 84
				}
				load_oob = ENG_DDAY
			}
			785 = {
				damage_building = {
					type = infrastructure
					damage = 3
				}
			}
			14 = {
				damage_building = {
					type = infrastructure
					damage = 3
				}
			}
			30 = {
				damage_building = {
					type = infrastructure
					damage = 3
				}
			}
		}
		
		remove_effect = {
			clr_country_flag = ww2_operation
		}
	}

	USA_operation_dragoon = {
			icon = generic_army_support
	
			available = { 
			is_ai = yes
			tag = USA
			date > 1944.1.1
			date < 1950.1.1
			has_war_with = GER
			surrender_progress < 0.1
			15 = { is_controlled_by_ROOT_or_ally = yes }
			29 = { is_controlled_by_ROOT_or_ally = yes }
			785 = { is_controlled_by_ROOT_or_ally = yes }
			}
	
			visible = {
			tag = USA
			NOT = { has_country_flag = ww2_operation }
			}
	
			cost = 0
	
			fire_only_once = yes
	
			ai_will_do = {
				factor = 1000
				modifier = {
					factor = 0
					date < 1944.8.15
				}
			}
	
		days_remove = 1

		complete_effect = {
			set_country_flag = ww2_operation
			USA = {
				load_oob = USA_DRAGOON
			}
			FRA = {
				load_oob = FRA_DRAGOON
			}
		}
		
		remove_effect = {
			clr_country_flag = ww2_operation
		}
	}

	ENG_operation_market_garden = {
		icon = generic_army_support

		available = { 
			is_ai = yes
			tag = ENG
			date > 1944.1.1
			date < 1950.1.1
			has_war_with = GER
			surrender_progress < 0.1
			15 = { is_controlled_by_ROOT_or_ally = yes }
			29 = { is_controlled_by_ROOT_or_ally = yes }
			785 = { is_controlled_by_ROOT_or_ally = yes }
			OR = {
				6 = { is_controlled_by_ROOT_or_ally = yes }
				34 = { is_controlled_by_ROOT_or_ally = yes }
			}
		}

		visible = {
			is_ai = yes
			tag = ENG
			NOT = { has_country_flag = ww2_operation }
		}

		cost = 0

		fire_only_once = yes

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				date < 1944.9.17
			}
		}


		days_remove = 1

		complete_effect = {
			set_country_flag = ww2_operation
			load_oob = ENG_MARKET_GARDEN
		}
		
		remove_effect = {
			clr_country_flag = ww2_operation
		}
	}

}
