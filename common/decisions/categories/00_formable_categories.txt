formable_nations_mod_category = {
	icon = generic_formable_nations
	scripted_gui = formable_selection_gui
	allowed = {
		NOT = { tag = ZOM }
		#Temporarily Hide Formable Nations While I work on them
		#always = no
	}

	available = {
		NOT = { tag = ZOM }
		#Temporarily Hide Formable Nations While I work on them
		#always = no
	}

	visible = {
		NOT = { tag = ZOM }
		#Temporarily Hide Formable Nations While I work on them
		#always = no
	}

}
