RON_game_settings = {

	RON_change_era_event = {
		icon = generic_form_nation
		allowed = { 
		RON_is_allowed_to_trigger_important_decisions = yes
		}
		available = {
		RON_is_allowed_to_trigger_important_decisions = yes
		custom_trigger_tooltip = {
			tooltip = TT_era_needs_changing
			RON_must_change_era = yes
		}
		NOT = { has_global_flag = change_era_in_progress }
		}
		cost = 0
		ai_will_do = {
			factor = 1000
		}
		days_re_enable = 30
		visible = {
		}
		complete_effect = {
			country_event = { id = change_era.1 }	
			set_global_flag = change_era_in_progress
		}
	}

	RON_report_on_powerstatus = {
		icon = gre_investment_decisions
		allowed = { 
		is_ai = no
		}
		available = {
		is_ai = no
		custom_trigger_tooltip = {
			tooltip = TT_power_status_needs_changing
			RON_must_change_power_status = yes	
		}
		NOT = { has_global_flag = power_status_in_progress }
		}
		cost = 0
		ai_will_do = {
			factor = 1000
		}
		days_re_enable = 30
		visible = {
		}
		complete_effect = {
			country_event = { id = power_status.1 }	
			set_global_flag = power_status_in_progress
		}
	}

	GER_censor_nazi_flag = {
		icon = generic_arrest
		allowed = { is_ai = no }
		available = {
			country_exists = GER
			GER = {
				has_government = fascism
			}
			custom_trigger_tooltip = {
			tooltip = TT_change_nazi_flag
			OR = {
				AND = {
					GER = {
						is_ai = yes
					}
					NOT = {
						tag = GER
					}
				}
				AND = {
					GER = {
						is_ai = no
					}
					tag = GER
				}
			}
			}
		}
		cost = 0
		ai_will_do = {
			factor = 0
		}
		fire_only_once = yes
		visible = {
		}
		complete_effect = {
			GER = {
				set_cosmetic_tag = GER_CENSORED
			}
		}
	}
}
