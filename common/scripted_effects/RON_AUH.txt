# Austria-Hungary : the loyalty of the eleven nationalities, and what it costs to lose it.
#
# calculate_uprising runs monthly, for austria-hungary with an open settlement.
#
# Under 35 the nationality stops being Austro-Hungarian in law: the state loses the
# Austro-Hungarian core. Under 20 it stops being so in fact: the compliance drops to zero.
# Back over 35 the core is granted again on any state Vienna still controls.

calculate_uprising = {
	if = {
		limit = {
			check_variable = {
				SER_support < 35
			}
		}
		every_state = {
			limit = {
				is_core_of = SER
				is_core_of = AUH
			}
			remove_core_of = AUH
		}
		if = {
			limit = {
				check_variable = {
					SER_support < 20
				}
			}
			every_state = {
				limit = {
					is_controlled_by = AUH
					is_core_of = SER
				}
				set_compliance = 0
			}
		}
	}
	else_if = {
		limit = {
			check_variable = {
				SER_support > 34
			}
		}
		every_state = {
			limit = {
				is_core_of = SER
				is_controlled_by = AUH
			}
			add_core_of = AUH
		}
	}

	if = {
		limit = {
			check_variable = {
				CRO_support < 35
			}
		}
		every_state = {
			limit = {
				is_core_of = CRO
				is_core_of = AUH
			}
			remove_core_of = AUH
		}
		if = {
			limit = {
				check_variable = {
					CRO_support < 20
				}
			}
			every_state = {
				limit = {
					is_controlled_by = AUH
					is_core_of = CRO
				}
				set_compliance = 0
			}
		}
	}
	else_if = {
		limit = {
			check_variable = {
				CRO_support > 34
			}
		}
		every_state = {
			limit = {
				is_core_of = CRO
				is_controlled_by = AUH
			}
			add_core_of = AUH
		}
	}

	if = {
		limit = {
			check_variable = {
				BOS_support < 35
			}
		}
		every_state = {
			limit = {
				is_core_of = BOS
				is_core_of = AUH
			}
			remove_core_of = AUH
		}
		if = {
			limit = {
				check_variable = {
					BOS_support < 20
				}
			}
			every_state = {
				limit = {
					is_controlled_by = AUH
					is_core_of = BOS
				}
				set_compliance = 0
			}
		}
	}
	else_if = {
		limit = {
			check_variable = {
				BOS_support > 34
			}
		}
		every_state = {
			limit = {
				is_core_of = BOS
				is_controlled_by = AUH
			}
			add_core_of = AUH
		}
	}

	if = {
		limit = {
			check_variable = {
				AUS_support < 35
			}
		}
		every_state = {
			limit = {
				is_core_of = AUS
				is_core_of = AUH
			}
			remove_core_of = AUH
		}
		if = {
			limit = {
				check_variable = {
					AUS_support < 20
				}
			}
			every_state = {
				limit = {
					is_controlled_by = AUH
					is_core_of = AUS
				}
				set_compliance = 0
			}
		}
	}
	else_if = {
		limit = {
			check_variable = {
				AUS_support > 34
			}
		}
		every_state = {
			limit = {
				is_core_of = AUS
				is_controlled_by = AUH
			}
			add_core_of = AUH
		}
	}

	if = {
		limit = {
			check_variable = {
				CZE_support < 35
			}
		}
		every_state = {
			limit = {
				is_core_of = CZE
				is_core_of = AUH
			}
			remove_core_of = AUH
		}
		if = {
			limit = {
				check_variable = {
					CZE_support < 20
				}
			}
			every_state = {
				limit = {
					is_controlled_by = AUH
					is_core_of = CZE
				}
				set_compliance = 0
			}
		}
	}
	else_if = {
		limit = {
			check_variable = {
				CZE_support > 34
			}
		}
		every_state = {
			limit = {
				is_core_of = CZE
				is_controlled_by = AUH
			}
			add_core_of = AUH
		}
	}

	if = {
		limit = {
			check_variable = {
				HUN_support < 35
			}
		}
		every_state = {
			limit = {
				is_core_of = HUN
				is_core_of = AUH
			}
			remove_core_of = AUH
		}
		if = {
			limit = {
				check_variable = {
					HUN_support < 20
				}
			}
			every_state = {
				limit = {
					is_controlled_by = AUH
					is_core_of = HUN
				}
				set_compliance = 0
			}
		}
	}
	else_if = {
		limit = {
			check_variable = {
				HUN_support > 34
			}
		}
		every_state = {
			limit = {
				is_core_of = HUN
				is_controlled_by = AUH
			}
			add_core_of = AUH
		}
	}

	if = {
		limit = {
			check_variable = {
				POL_support < 35
			}
		}
		every_state = {
			limit = {
				is_core_of = POL
				is_core_of = AUH
			}
			remove_core_of = AUH
		}
		if = {
			limit = {
				check_variable = {
					POL_support < 20
				}
			}
			every_state = {
				limit = {
					is_controlled_by = AUH
					is_core_of = POL
				}
				set_compliance = 0
			}
		}
	}
	else_if = {
		limit = {
			check_variable = {
				POL_support > 34
			}
		}
		every_state = {
			limit = {
				is_core_of = POL
				is_controlled_by = AUH
			}
			add_core_of = AUH
		}
	}

	if = {
		limit = {
			check_variable = {
				ROM_support < 35
			}
		}
		every_state = {
			limit = {
				is_core_of = ROM
				is_core_of = AUH
			}
			remove_core_of = AUH
		}
		if = {
			limit = {
				check_variable = {
					ROM_support < 20
				}
			}
			every_state = {
				limit = {
					is_controlled_by = AUH
					is_core_of = ROM
				}
				set_compliance = 0
			}
		}
	}
	else_if = {
		limit = {
			check_variable = {
				ROM_support > 34
			}
		}
		every_state = {
			limit = {
				is_core_of = ROM
				is_controlled_by = AUH
			}
			add_core_of = AUH
		}
	}

	if = {
		limit = {
			check_variable = {
				SLV_support < 35
			}
		}
		every_state = {
			limit = {
				is_core_of = SLV
				is_core_of = AUH
			}
			remove_core_of = AUH
		}
		if = {
			limit = {
				check_variable = {
					SLV_support < 20
				}
			}
			every_state = {
				limit = {
					is_controlled_by = AUH
					is_core_of = SLV
				}
				set_compliance = 0
			}
		}
	}
	else_if = {
		limit = {
			check_variable = {
				SLV_support > 34
			}
		}
		every_state = {
			limit = {
				is_core_of = SLV
				is_controlled_by = AUH
			}
			add_core_of = AUH
		}
	}

	if = {
		limit = {
			check_variable = {
				UKR_support < 35
			}
		}
		every_state = {
			limit = {
				is_core_of = UKR
				is_core_of = AUH
			}
			remove_core_of = AUH
		}
		if = {
			limit = {
				check_variable = {
					UKR_support < 20
				}
			}
			every_state = {
				limit = {
					is_controlled_by = AUH
					is_core_of = UKR
				}
				set_compliance = 0
			}
		}
	}
	else_if = {
		limit = {
			check_variable = {
				UKR_support > 34
			}
		}
		every_state = {
			limit = {
				is_core_of = UKR
				is_controlled_by = AUH
			}
			add_core_of = AUH
		}
	}

	########################################################################################
	# SLO and ITA start at 35 and 40 in the history file
	#
	# Order matters and is deliberate. State 70 is a core of CZE and of SLO at once, so the
	# last block to run decides whether Vienna still holds it in law; SLO is placed after CZE
	# so that the Slovak column, and not the Bohemian one, answers for the Uplands.
	#
	# ITA is read as is_core_of = ITA, like every other tag here. Inside the Monarchy that is
	# the Trentino, South Tyrol, Trieste, Istria and Fiume, which is the intent. Outside it,
	# the granting branch behaves as the ROM and SER blocks already do and will core Italian
	# ground the Monarchy has conquered while the Italians are content - a consequence of the
	# pattern, not of this lot.
	########################################################################################

	if = {
		limit = {
			check_variable = {
				SLO_support < 35
			}
		}
		every_state = {
			limit = {
				is_core_of = SLO
				is_core_of = AUH
			}
			remove_core_of = AUH
		}
		if = {
			limit = {
				check_variable = {
					SLO_support < 20
				}
			}
			every_state = {
				limit = {
					is_controlled_by = AUH
					is_core_of = SLO
				}
				set_compliance = 0
			}
		}
	}
	else_if = {
		limit = {
			check_variable = {
				SLO_support > 34
			}
		}
		every_state = {
			limit = {
				is_core_of = SLO
				is_controlled_by = AUH
			}
			add_core_of = AUH
		}
	}

	if = {
		limit = {
			check_variable = {
				ITA_support < 35
			}
		}
		every_state = {
			limit = {
				is_core_of = ITA
				is_core_of = AUH
			}
			remove_core_of = AUH
		}
		if = {
			limit = {
				check_variable = {
					ITA_support < 20
				}
			}
			every_state = {
				limit = {
					is_controlled_by = AUH
					is_core_of = ITA
				}
				set_compliance = 0
			}
		}
	}
	else_if = {
		limit = {
			check_variable = {
				ITA_support > 34
			}
		}
		every_state = {
			limit = {
				is_core_of = ITA
				is_controlled_by = AUH
			}
			add_core_of = AUH
		}
	}
}
